<div class='film-selection'>
  <div class='film-selection_name'>
    {{collection_name}}
  </div>
  <ul class='film-selection_films'>
    {{#if haveFilms}}{{else}}
      <div><p>К сожалению, но ничего не найдено.</p></div>
    {{/if}}
  </ul>
</div>